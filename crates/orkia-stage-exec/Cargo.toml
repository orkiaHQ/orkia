[package]
name = "orkia-stage-exec"
version = "0.1.0"
edition.workspace = true
license = "Elastic-2.0"
description = "Generic interactive PTY stage executor: spawn one agent stage from a resolved plan, capture its output (SPEC single-shell pipeline)"

[dependencies]
orkia-shell-types = { workspace = true }
orkia-shell = { workspace = true }
orkia-terminal-core = { workspace = true }
libc = { workspace = true }
tokio = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tempfile = "3"
