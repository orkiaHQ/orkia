[package]
name = "orkia-model-catalog"
version = "0.1.0"
edition.workspace = true
rust-version = "1.85"
license.workspace = true
description = "Canonical model capability catalog — shared seed data for model routing"

[dependencies]
serde = { workspace = true }
